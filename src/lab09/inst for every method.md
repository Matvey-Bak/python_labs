# Объяснение методов класса Group

## 📋 **Общая архитектура класса**
Класс `Group` реализует **CRUD-репозиторий** для работы со студентами, хранящимися в CSV-файле. Каждый метод отвечает за конкретную операцию.

---

## 🔧 **1. Конструктор `__init__`**
```python
def __init__(self, storage_path: str):
```
### **Зачем нужен:**
- **Инициализация объекта** группы студентов
- **Создание/проверка** файла-хранилища при создании объекта

### **Что делает:**
1. Принимает путь к CSV-файлу
2. Преобразует его в объект `Path` для удобной работы
3. Вызывает `_ensure_storage_exists()` для гарантии существования валидного файла

### **Пример использования:**
```python
group = Group("students.csv")  # Создаёт или открывает файл students.csv
```

---

## 📁 **2. Внутренний метод `_ensure_storage_exists`**
```python
def _ensure_storage_exists(self):
```
### **Зачем нужен:**
- **Гарантировать** существование CSV-файла с правильной структурой
- **Автоматическое создание** файла при первом использовании

### **Что делает:**
1. Проверяет, существует ли файл или он пустой
2. Если нет — создаёт файл с заголовками столбцов
3. Устанавливает структуру данных: `fio,birthdate,group,gpa`

### **Почему приватный (`_`):**
- Внутренняя логика, не должна вызываться извне
- Гарантирует корректное состояние файла при любых операциях

---

## 📖 **3. Метод чтения `_read_all`**
```python
def _read_all(self) -> list[dict]:
```
### **Зачем нужен:**
- **Чтение всех данных** из CSV в память
- **Подготовка данных** для других методов

### **Что делает:**
1. Открывает CSV-файл для чтения
2. Использует `csv.DictReader` для преобразования строк в словари
3. Игнорирует пустые строки
4. Возвращает список словарей (каждый словарь = студент)

### **Почему приватный:**
- Низкоуровневая операция чтения
- Все публичные методы используют этот метод как основу

---

## 📝 **4. Метод записи `_write_all`**
```python
def _write_all(self, rows: list[dict]):
```
### **Зачем нужен:**
- **Сохранение всех данных** обратно в CSV
- **Атомарная запись** — либо все данные записываются, либо ничего

### **Что делает:**
1. Открывает файл для записи (перезаписывает полностью)
2. Записывает заголовок столбцов
3. Записывает все строки данных
4. Гарантирует корректный формат CSV

### **Важность:**
- Централизованная точка записи
- Предотвращает частичную запись данных
- Обеспечивает целостность файла

---

## 👥 **5. Основной CRUD метод `list`**
```python
def list(self) -> list[Student]:
```
### **Зачем нужен:**
- **Получение всех студентов** в удобном формате
- **READ** операция из CRUD

### **Что делает:**
1. Читает все записи из CSV через `_read_all()`
2. Преобразует каждую запись в объект `Student`
3. Пропускает некорректные записи с предупреждением
4. Возвращает список объектов

### **Особенности:**
- Валидация данных при чтении
- Автоматическое преобразование типов (например, `gpa` из строки в float)

---

## ➕ **6. Метод создания `add`**
```python
def add(self, student: Student):
```
### **Зачем нужен:**
- **Добавление нового студента** в хранилище
- **CREATE** операция из CRUD

### **Что делает:**
1. Проверяет, нет ли уже студента с таким ФИО
2. Создаёт новую запись из объекта `Student`
3. Добавляет запись в общий список
4. Сохраняет все данные через `_write_all()`

### **Защита от дубликатов:**
```python
if row['fio'] == student.fio:
    raise ValueError(f"Student already exists")
```

---

## 🔍 **7. Метод поиска `find`**
```python
def find(self, substr: str) -> list[Student]:
```
### **Зачем нужен:**
- **Поиск студентов** по части ФИО
- **Гибкий поиск** без точного совпадения

### **Что делает:**
1. Регистронезависимый поиск подстроки в ФИО
2. Использует list comprehension для фильтрации
3. Преобразует найденные записи в объекты `Student`

### **Примеры поиска:**
```python
group.find("Иванов")  # Найдёт "Иванов И.И.", "Петрова (Иванова) А."
group.find("ива")     # Найдёт "Иванов", "Сиваков", "Иванова"
```

---

## ❌ **8. Метод удаления `remove`**
```python
def remove(self, fio: str):
```
### **Зачем нужен:**
- **Удаление студента** по точному ФИО
- **DELETE** операция из CRUD

### **Что делает:**
1. Находит все записи с указанным ФИО
2. Удаляет их из списка
3. Сохраняет обновлённые данные
4. Возвращает `True` если удаление прошло успешно

### **Особенности:**
- Точное совпадение ФИО (регистрозависимое)
- Удаляет ВСЕ записи с таким ФИО (на случай дубликатов)
- Возвращает булев результат для обратной связи

---

## ✏️ **9. Метод обновления `update`**
```python
def update(self, fio: str, **fields):
```
### **Зачем нужен:**
- **Изменение данных** существующего студента
- **UPDATE** операция из CRUD

### **Что делает:**
1. Находит студента по точному ФИО
2. Проверяет, какие поля можно обновлять
3. Обновляет указанные поля
4. Сохраняет изменения

### **Гибкость обновления:**
```python
# Можно обновить любое сочетание полей
group.update("Иванов", gpa=5.0)
group.update("Петрова", group="ИС-102", birthdate="2002-01-01")
```

---

## 🧹 **10. Дополнительный метод `clear`**
```python
def clear(self):
```
### **Зачем нужен:**
- **Полная очистка** файла с данными
- **Тестирование** — подготовка чистого состояния
- **Сброс данных** — удаление всех записей

### **Что делает:**
1. Вызывает `_write_all([])` с пустым списком
2. Сохраняет только заголовок CSV

---

## 🔢 **11. Метод подсчёта `count`**
```python
def count(self) -> int:
```
### **Зачем нужен:**
- **Быстрое получение** количества студентов
- **Статистика** — без загрузки всех объектов в память

### **Что делает:**
1. Читает все записи через `_read_all()`
2. Возвращает длину списка
3. Эффективнее, чем `len(group.list())`

---

## 👤 **12. Метод точного поиска `get_by_fio`**
```python
def get_by_fio(self, fio: str) -> Student | None:
```
### **Зачем нужен:**
- **Получение конкретного студента** по точному ФИО
- **Альтернатива** `find()` для точного совпадения
- **Проверка существования** студента

### **Что делает:**
1. Ищет запись с точным совпадением ФИО
2. Преобразует в объект `Student`
3. Возвращает объект или `None` если не найден

### **Отличие от `find`:**
- `find("Ива")` → список всех с подстрокой "Ива"
- `get_by_fio("Иванов Иван")` → один объект или `None`

---

## 📊 **Сводная таблица методов**

| Метод | CRUD операция | Возвращает | Назначение |
|-------|---------------|------------|------------|
| `list()` | READ | `list[Student]` | Все студенты |
| `add(student)` | CREATE | `None` | Добавить студента |
| `find(substr)` | READ | `list[Student]` | Найти по подстроке |
| `remove(fio)` | DELETE | `bool` | Удалить по ФИО |
| `update(fio, **fields)` | UPDATE | `bool` | Обновить данные |
| `count()` | - | `int` | Количество студентов |
| `get_by_fio(fio)` | READ | `Student \| None` | Найти точное ФИО |
| `clear()` | DELETE | `None` | Очистить всё |

---

## 🔄 **Взаимосвязь методов**

```
          __init__
             ↓
    _ensure_storage_exists
             ↓
    ↗-----------↘
_read_all   _write_all
    ↓           ↑
list()      add()/update()/remove()
    ↓
find() → get_by_fio()
    ↓
count()
    ↓
clear()
```

### **Центральные методы:**
- `_read_all()` и `_write_all()` — низкоуровневые операции ввода-вывода
- Все CRUD методы строятся на их основе
- Изменение формата хранения потребует правки только этих двух методов

---

## 🎯 **Принципы проектирования**

### **1. Единая ответственность (Single Responsibility)**
Каждый метод делает ровно одну вещь:
- `add()` — только добавление
- `find()` — только поиск
- `update()` — только обновление

### **2. Разделение чтения и записи**
- `_read_all()` только читает
- `_write_all()` только пишет
- Упрощает отладку и тестирование

### **3. Защищённые внутренние методы**
- `_ensure_storage_exists()`, `_read_all()`, `_write_all()`
- Нельзя вызвать извне, только из других методов класса
- Гарантируют корректное состояние данных

### **4. Консистентный интерфейс**
- Все публичные методы работают с объектами `Student`
- Возвращают предсказуемые типы
- Исключения для ошибок, `None` для "не найдено"

---

## 💡 **Пример использования всех методов вместе**

```python
# 1. Создание группы
group = Group("students.csv")

# 2. Добавление студентов
group.add(Student("Иванов Иван", "2000-01-01", "ИС-101", 4.5))
group.add(Student("Петрова Анна", "2001-02-02", "ИС-101", 4.8))

# 3. Получение всех
all_students = group.list()  # [Student1, Student2]

# 4. Поиск
found = group.find("ива")    # [Student("Иванов Иван")]

# 5. Обновление
group.update("Иванов Иван", gpa=5.0)

# 6. Проверка
student = group.get_by_fio("Иванов Иван")  # GPA теперь 5.0

# 7. Подсчёт
total = group.count()  # 2

# 8. Удаление
group.remove("Петрова Анна")

# 9. Очистка (например, для тестов)
group.clear()
```

---

## 🛡️ **Обработка ошибок и валидация**

Каждый метод включает валидацию:
- `add()` — проверка на дубликаты
- `list()` — пропуск некорректных строк
- `update()` — проверка допустимых полей
- Все методы — обработка отсутствия файла

Это делает класс **надёжным** и **готовым к реальному использованию** с потенциально повреждёнными данными.